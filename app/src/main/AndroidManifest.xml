<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.deyvi.gerenciamentoderepublica">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".application.GerenciadorAluguelApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="AA_DB_NAME"
            android:value="GERIR_ALUGEL.db" />
        <meta-data
            android:name="AA_DB_VERSION"
            android:value="1" />
        <!-- <meta-data -->
        <!-- android:name=" AA_MODELS " -->
        <!-- android:value="com.example.deyvi.gerenciamentoderepublica.entitys" /> -->

        <activity
            android:name=".activitys.MainActivity_"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activitys.Splash_" />
        <activity android:name=".activitys.BaseActivity_" />
        <activity
            android:name=".activitys.CadastroActivity_"
            android:label="Cadastro" />
        <activity
            android:name=".activitys.WelcomeActivity_"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activitys.ScrollingActivity_"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activitys.CadastroQuartoActivity_"></activity>
    </application>

</manifest>
